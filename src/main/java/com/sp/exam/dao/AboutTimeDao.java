package com.sp.exam.dao;

import com.sp.exam.pojo.AboutTime;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AboutTimeDao extends JpaRepository<AboutTime,String> {
}
